{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main status window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
